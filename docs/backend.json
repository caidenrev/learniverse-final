{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the Learniverse application.",
      "properties": {
        "uid": {
          "type": "string",
          "description": "Unique identifier for the user, sourced from Firebase Auth UID."
        },
        "email": {
          "type": "string",
          "description": "The user's email address.",
          "format": "email"
        },
        "displayName": {
            "type": "string",
            "description": "The user's display name."
        },
        "photoURL": {
            "type": "string",
            "description": "URL of the user's profile picture."
        },
        "subscriptionId": {
          "type": "string",
          "description": "Reference to the user's current subscription in the 'subscriptions' collection."
        },
        "createdAt": {
          "type": "string",
          "description": "The timestamp when the user account was created.",
          "format": "date-time"
        }
      },
      "required": [
        "uid",
        "email",
        "createdAt"
      ]
    },
    "Subscription": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Subscription",
      "type": "object",
      "description": "Represents a user's subscription plan and associated limits.",
      "properties": {
        "planId": {
          "type": "string",
          "description": "The type of subscription plan (e.g., 'free', 'premium')."
        },
        "status": {
            "type": "string",
            "description": "Status of the subscription (e.g., 'active', 'canceled', 'past_due')."
        },
        "currentPeriodEnd": {
            "type": "string",
            "format": "date-time",
            "description": "The timestamp when the current subscription period ends."
        },
        "usage": {
            "type": "object",
            "description": "Tracks the user's feature usage for the current billing period.",
            "properties": {
                "summaryCount": {"type": "number"},
                "paraphraseCount": {"type": "number"},
                "tutorQuestionCount": {"type": "number"},
                "lastResetDate": {
                    "type": "string",
                    "format": "date-time"
                }
            }
        }
      },
      "required": [
        "planId",
        "status"
      ]
    }
  },
  "auth": {
    "providers": [
      "google.com"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/User"
      },
      "description": "Stores user profile information, linked to their Firebase Auth UID."
    },
    "/users/{userId}/subscription": {
        "schema": {
            "$ref": "#/entities/Subscription"
        },
        "description": "Stores the subscription status and usage limits for a specific user. This is a subcollection under each user."
    }
  }
}